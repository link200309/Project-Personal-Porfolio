const SectionTitle = ({ title }: { title: string }) => {
  return (
    <h2 className="text-center text-display-sm font-bold text-primary-pri2">
      {title}
    </h2>
  );
};

export default SectionTitle;
